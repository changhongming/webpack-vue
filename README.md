# Installation
```sh
$ npm install
```
# Usage
- developement bundle
  ```
  npm run dev
  ```
- production bundle
  ```
  npm run prod
  ```
- frontend hot-reload debug server
  ```
  npm run watch
  ```
- webpack bundle analyzer
  ```
  npm run analyzer
  ```
